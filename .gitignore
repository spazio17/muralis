# Bind mounts for the containerized build: SDK packages and Gradle caches. Both
# are regenerated by scripts/build-container.sh and must never be committed.
/.android-sdk/
/.gradle/

# Gradle outputs
/.gradle-build/
/app/build/
local.properties

# scripts/changelog.py is imported by its own tests and leaves a cache beside it.
__pycache__/
